extend type Query {
    paths: [Path]
}

type Path {
    id: ID!
    places: [Patient]
    value: Float
}

extend type Mutation {
    newPath(places: [PatientInput]) : Path!
    deletePath(id: ID!) : Boolean
    #    updatePath(id:ID!, location: [Float]) : Path!
}

input CoordinateInput {
    id: ID,
    location: [Float]
}
