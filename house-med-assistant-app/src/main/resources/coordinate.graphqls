extend type Query {
    coordinates: [Coordinate]
}

type Coordinate {
    id: ID!
    location: [Float]
}

extend type Mutation {
    newCoordinate(location: [Float]) : Coordinate!
    deleteCoordinate(id: ID!) : Boolean!
}